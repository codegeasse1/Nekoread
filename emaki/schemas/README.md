Schema export note:
- `io.aatricks.easyreader.data.local.AppDatabase/` is the active Room export path for the current `AppDatabase` class package and is used by migration tests.
- `io.aatricks.novelscraper.data.local.AppDatabase/` is retained intentionally as a historical snapshot from the pre-rename package path to keep older exported schemas available for reference.
