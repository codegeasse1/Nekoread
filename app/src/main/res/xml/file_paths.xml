<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Downloaded update APKs live under filesDir/updates/ (exposed to the installer via FileProvider). -->
    <files-path name="updates" path="updates/" />
</paths>
