package com.example.util

/**
 * Manual build marker â bump this string on every push so a screenshot instantly tells us which
 * APK the tester is actually running (last time the wrong build was installed and the error text
 * misled us). Shown in the Settings screen header.
 */
object BuildInfo {
    const val VERSION = "2026-09-09-v2.2.7-dx3"
}
